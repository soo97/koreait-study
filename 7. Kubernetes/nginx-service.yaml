apiVersion: v1
kind: Service
metadata:
  name: nginx-service

spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80        # service 내부 포트
      targetPort: 80  # pod 컨테이너 포트
      nodePort: 30080  # 외부에서 노드에 접근할 때 사용하는 포트